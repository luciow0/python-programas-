def busqueda(lista, elemento_buscado):
    pos = -1 
    i = 0
    while pos == -1 and i < len(lista):
        if lista[i] == elemento_buscado:
            pos = i
        i += 1
    return pos

if pos != 

